# 启用 AndroidX，与 androidx.appcompat / androidx.activity / androidx.core 依赖配套
android.useAndroidX=true

# Kotlin 代码风格：自动添加 @Metadata 注解，编译时无需额外配置
kotlin.code.style=official

# 非传递型依赖，避免依赖冲突
android.nonTransitiveRClass=true

# SDK 预构建缓存行为更稳
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
